## 2026-07-23

- 替换 App Icon：将 `/Users/ding/Downloads/easypasteicon.png` 转为 `.icns` 并放入 `Support/AppIcon.icns`
- 修改 `script/build_and_run.sh`：构建时自动将 icon 复制到 `Contents/Resources/`，并在生成的 Info.plist 中写入 `CFBundleIconFile`
- 修改根目录 `Info.plist`：同步加入 `CFBundleIconFile` 键
- 新增 Onboarding 引导：首次运行时展示6页引导页（欢迎/自动捕获/一键唤起/智能整理/搜索与预览/准备就绪）
  - `Models/AppSettings.swift`：Snapshot 新增 `hasCompletedOnboarding` 字段（`decodeIfPresent` 兼容旧版存档）
  - `Views/OnboardingView.swift`：macOS 不使用 `.page(indexDisplayMode:)`（不可用），改用手动 ZStack + transition + direction 实现页面滑动切换
  - `Services/OnboardingWindowController.swift`：管理引导 NSWindow，居中显示，完成后写入 `hasCompletedOnboarding`
  - `App/EasyPasteApp.swift`：`AppServices.boot()` 末尾在 `!hasCompletedOnboarding` 时延迟 0.3s 弹出引导窗口
- 修复设置窗口无法从面板弹出的问题：
  - 根因：`PanelController.openSettingsFromPanel()` 使用 `sendAction(Selector(("showSettingsWindow:")))` 在 macOS 15 上不可靠；且面板（`.floating` 层级）在设置窗口创建时仍可见，可能遮挡设置窗口
  - `App/EasyPasteApp.swift`：新增 `SettingsActionCapture` 视图，在 SwiftUI 场景层次（MenuBarExtra + Settings scene）内捕获 `@Environment(\.openSettings)` 并存入 `AppServices.openSettingsAction`，供 PanelController 使用
  - `Services/PanelController.swift`：新增 `openSettings` 闭包属性；重写 `openSettingsFromPanel()` 为4级回退：已有窗口置前 → 捕获的 openSettings 动作 → 主菜单菜单项查找触发 → sendAction 兜底；增加 0.2s 延迟等待面板隐藏动画完成
